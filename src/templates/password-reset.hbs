<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
      background-color: #ffffff;
    }
    .header {
      text-align: center;
      padding: 20px 0;
      border-bottom: 2px solid #dc3545;
    }
    .header h1 {
      margin: 0;
      color: #dc3545;
    }
    .content {
      padding: 30px 20px;
    }
    .button {
      display: inline-block;
      padding: 12px 24px;
      background-color: #dc3545;
      color: white !important;
      text-decoration: none;
      border-radius: 4px;
      margin: 20px 0;
      font-weight: bold;
    }
    .button:hover {
      background-color: #c82333;
    }
    .link-text {
      word-break: break-all;
      color: #dc3545;
      font-size: 14px;
    }
    .warning {
      background-color: #fff3cd;
      border-left: 4px solid #ffc107;
      padding: 12px;
      margin: 20px 0;
    }
    .footer {
      margin-top: 30px;
      padding-top: 20px;
      border-top: 1px solid #ddd;
      font-size: 12px;
      color: #666;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>{{t "passwordReset.heading" appName=(t "app.name")}}</h1>
    </div>
    <div class="content">
      <h2>{{t "passwordReset.title"}}</h2>
      <p>{{t "passwordReset.greeting"}}</p>
      <p>{{t "passwordReset.body" appName=(t "app.name")}}</p>
      <div style="text-align: center;">
        <a href="{{resetLink}}" class="button">{{t "passwordReset.button"}}</a>
      </div>
      <p>{{t "passwordReset.linkText"}}</p>
      <p class="link-text">{{resetLink}}</p>
      <p><strong>{{t "passwordReset.expiration" minutes=expirationMinutes}}</strong></p>
      <div class="warning">
        <strong>{{t "passwordReset.securityNotice.title"}}</strong> {{t "passwordReset.securityNotice.body"}}
      </div>
    </div>
    <div class="footer">
      <p>{{t "passwordReset.footer.ignore"}}</p>
      <p>{{t "passwordReset.footer.contact"}}</p>
      <p>{{{t "passwordReset.footer.copyright" appName=(t "app.name")}}}</p>
    </div>
  </div>
</body>
</html>
